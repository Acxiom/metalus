language: scala

matrix:
  include:
    - os: linux
    - os: osx
      osx_image: xcode9.3

scala:
  - 2.11.12

script:
  - mvn -DrepoToken=$coverallsToken clean scoverage:report install scala:doc scalastyle:check coveralls:report
