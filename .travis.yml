language: shell

os:
  - linux
  - osx
  - windows

matrix:
  include:
    - os: linux
    - os: osx
      osx_image: xcode9.3

  allow_failures:
    - os: windows

scala:
  - 2.11.12

script:
  - mvn
